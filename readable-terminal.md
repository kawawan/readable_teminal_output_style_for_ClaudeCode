---
name: 読みやすい端末出力
description: 端末で崩れず読める箇条書きの規約。Markdown は普通に使い、リスト記法だけ避ける。
keep-coding-instructions: true
---

# 端末で読みやすい出力

Claude Code の端末は Markdown を普通にレンダリングする。太字・見出し・表・コードブロックはそのまま Markdown で書けば正しく表示される。**唯一の問題は Markdown のリスト記法だけ**である。

癖：連続するリスト項目（`- ` `* ` `1. `、および半角数字始まりの `1)`）の間に空行を入れても、レンダラが「詰めたリスト（tight list）」と解釈して空行を消してしまう。入れ子のある項目だけ空くのでムラになる。

補足：行頭の半角スペースは構文と解釈され、字下げとして残らないことが多い。だからネストの字下げには全角スペースを使う。

一言でいう原則：**Markdown は普通に使う。ただしリスト記法だけ使わない。** 箇条書きを Markdown が記法と認識しない文字（`・`、全角の `１）`）で書けば、普通の段落として扱われ、段落間の空行が保持される。

この style の責務は端末での描画崩れの回避だけに絞る。読みやすさそのものの原則（一文一義・横を縦へ・対比は表・体言止め回避・箇条書きの余白）は `readable-docs` スキルの担当であり、地の文を書くときはそちらを参照する。両者は重ならない。

## ルール

**1. 箇条書きに Markdown のリスト記法を使わない**
`- ` `* ` `1. `、および半角数字始まりの `1)` は使わない。半角数字始まりはすべてリストとしてパースされ、空行を詰められる。

**2. 各項目を独立した段落として書き、項目間に必ず空行を入れる**
これは推奨ではなく必須。`・` で始まる行どうしを空行なしで並べると、Markdown が1つの段落に結合してしまい、結局詰まって見える。項目と項目の間には必ず空行を1行入れる。

**3. 行頭記号は「・」、番号は全角「１）」を使う**
箇条書きの記号は `-` ではなく `・`。番号付きは半角の `1.` や `1)` ではなく、全角の `１）` `２）` を使う。全角数字は先頭が半角数字でないためリストと認識されず、段落テキストとして空行が保持される。

**4. ネストは「・」の前に全角スペースを置いて字下げする**
サブ項目は行頭に全角スペース（U+3000）を置いてぶら下げる。全角スペースは文字扱いなので字下げが保持される。行頭の半角スペースは残らないので使わない。

**5. 表は普通の Markdown テーブルをそのまま使う**
比較・対応・多属性の一覧は Markdown のテーブル記法（`| … |`）で出す。テーブルは端末レンダラで枠付きの箱として正しく描画される。前後に空行を置き、行頭を字下げしないこと（そうすればレンダラが表として認識する）。横並びで見せたいものはむしろ積極的に表にする。

**6. コードは通常どおりコードブロックで出す**

## 例

**１）A/Bの2条件を実行**
各テストでサブエージェントを2つ起動する。

　　・with_skill（スキルあり）

　　・without_skill（スキルなし）

　　→ 対照実験で改善を見るのがポイント。

**２）結果を集計する**
条件ごとにスコアを平均して比較する。

比較・対応づけは表で出す：

| やりたいこと | ダメな書き方 | 正しい書き方 |
| --- | --- | --- |
| 項目間を1行空ける | `- ` `1. ` `1)` のリスト | `・` 段落＋空行／`１）` 段落＋空行 |
| サブ項目を字下げ | 半角スペース＋`-` | 全角スペース＋`・` |
| 記号 | `-` `*` `1)` | `・` `１）` `→` |

## 適用範囲（重要）

線引きは「どこでレンダリングされるか」で決まる。

Claude Code の端末で表示されるもの（チャット回答・Plan ファイルなど）には、上記の規約を適用する。

標準の Markdown レンダラや GitHub／Web で見るもの（README・配布ドキュメントなど）は、普通の `- `／`1. ` リストで正しく表示されるので通常の Markdown を使い、`・` や全角スペース、全角番号 `１）` を持ち込まない。
